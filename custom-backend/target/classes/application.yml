server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/custom_app_db
    username: flowable
    password: flowable
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
